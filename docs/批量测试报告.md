# 批量测试报告

测试日期：2026-08-14  
测试对象：`skill-platform-architecture-compliant` 副本  
本地地址：前端 `http://127.0.0.1:5177`，后端 `http://127.0.0.1:8083/race-api`

## 结果摘要

| 范围 | 结果 | 说明 |
| --- | --- | --- |
| 后端构建 | 通过 | `mvn -q -DskipTests package` |
| 前端类型检查 | 通过 | `npm run typecheck` |
| 前端单元测试 | 通过 | 6/6 通过 |
| 前端生产构建 | 通过 | `npm run build` |
| 前端代码规范 | 通过（警告） | `npm run lint`，0 errors、1301 历史风格 warnings |
| API 批量验收 | 通过 | `scripts/verify-platform.sh` |
| 评审定版链路 | 通过 | 提交后为 `reviewing` 且无 Git tag；通过后为 `released` 且创建 Git tag/发布包 |
| 企业 Git 内网导入 | 通过 | 本机 HTTP Git 服务真实克隆、连接校验、分支读取和 Markdown `SKILL.md` 解析 |
| 评测页真实接口 | 通过 | 创建评测任务并将状态从 `pending` 更新为 `running` |

## 已覆盖业务点

1. 登录成功、错误密码拒绝、未登录 API 返回 401。
2. Skill 创建、服务端禁止公共可见性并降级为私有、标准目录保存。
3. `SKILL.md` / `requirements.txt` / `references/` / `scripts/` 文件结构读取与目录展开。
4. 保存草稿、修订号冲突拒绝、刷新后保存、草稿 Git 提交与 ZIP 快照。
5. 单条调试成功、非法 JSON、Python 运行异常和完整 stderr 日志。
6. 运行全部业务用例；实际回归结果为 4/4 通过。
7. 调试过程 Token 显示、单条日志下载和批量下载入口。
8. 提交评审不定版；审核通过才同步 `SKILL.md`、Git Tag 与 NAS 发布 ZIP。
9. 企业 Git 凭证创建、掩码返回、后端托管、连接测试、分支读取、Markdown Skill 导入、删除与团队越权拒绝。
10. 智能体创建、画布配置、子智能体、Skill 挂载。
11. 评测任务创建/状态更新、流水线创建/更新/查询/删除。
12. 页面可访问性：技能管理、技能开发、智能体、评测、factory/skill 流水线、团队智能体和团队页面。

## 修复记录

- 本地 H2 初始化脚本补齐 `eval_task`、`pipeline` 两张表，恢复评测与流水线功能。
- 前端保存代码时传递并更新 `draftRevision`，使多人编辑的乐观锁真正生效。
- 管理页创建表单移除“公共”可见范围，和后端禁止公共 Skill 的规则一致。
- 增加可重复执行的 `scripts/verify-platform.sh`。
- 修复企业 Git 的 dumb HTTP 传输兼容性：连接校验和分支读取改为后端短暂 clone，避免 JGit `ls-remote` 空指针。
- 评测页由静态成功提示改为真实创建/读取后端评测任务；新增独立审核中心。

## 已知部署前置条件

- AI 真实生成依赖企业内部 OpenAI 兼容模型地址与后端环境变量 `TOKEN_PLAN_API_KEY`。本次离线环境未提供可访问的企业模型服务，已验证接口会返回明确错误；未使用浏览器或代码保存任何模型密钥。因此“模型的真实语义生成”不能被标记为已通过。
- 调试运行器当前为受限本地进程，不是容器级安全沙箱。生产运行不可信脚本前，应接入企业容器沙箱。
- 前端构建存在大包警告和既有风格 warnings，不影响本次功能验收；后续可拆分编辑器/图形依赖以降低首屏体积。
