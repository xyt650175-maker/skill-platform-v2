<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.llmaif.skills.dao.PipelineDAO">
    <sql id="baseColumn">id, name, description, type, stages, current_version, status, creator_id, is_deleted, create_time, update_time</sql>
    <select id="selectByName" resultType="com.company.llmaif.skills.dao.entity.PipelineEntity">
        SELECT <include refid="baseColumn"/> FROM `pipeline` WHERE name = #{name} AND is_deleted = 0
    </select>
    <select id="selectByType" resultType="com.company.llmaif.skills.dao.entity.PipelineEntity">
        SELECT <include refid="baseColumn"/> FROM `pipeline` WHERE type = #{type} AND is_deleted = 0 ORDER BY create_time DESC
    </select>
</mapper>
