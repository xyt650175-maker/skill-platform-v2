<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.llmaif.skills.dao.EvalTaskDAO">
    <sql id="baseColumn">id, name, agent_id, agent_version, dataset_key, dataset_version, scope, status, result_summary, creator_id, is_deleted, create_time, update_time</sql>
    <select id="selectByAgentId" resultType="com.company.llmaif.skills.dao.entity.EvalTaskEntity">
        SELECT <include refid="baseColumn"/> FROM `eval_task` WHERE agent_id = #{agentId} AND is_deleted = 0 ORDER BY create_time DESC
    </select>
    <select id="selectByStatus" resultType="com.company.llmaif.skills.dao.entity.EvalTaskEntity">
        SELECT <include refid="baseColumn"/> FROM `eval_task` WHERE status = #{status} AND is_deleted = 0 ORDER BY create_time DESC
    </select>
</mapper>
