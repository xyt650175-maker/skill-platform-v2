<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.llmaif.skills.dao.SkillMountingDAO">

    <sql id="baseColumn">
        id, agent_id, sub_agent_id, skill_id, skill_alias, agent_version, enabled,
        is_deleted, create_time, update_time
    </sql>

    <select id="selectByAgentId" resultType="com.company.llmaif.skills.dao.entity.SkillMountingEntity">
        SELECT <include refid="baseColumn"/>
        FROM `skill_mounting`
        WHERE agent_id = #{agentId} AND is_deleted = 0
        ORDER BY create_time DESC
    </select>

    <select id="selectByAgentIdAndVersion" resultType="com.company.llmaif.skills.dao.entity.SkillMountingEntity">
        SELECT <include refid="baseColumn"/>
        FROM `skill_mounting`
        WHERE agent_id = #{agentId} AND agent_version = #{agentVersion} AND is_deleted = 0
        ORDER BY create_time DESC
    </select>

    <select id="selectBySkillId" resultType="com.company.llmaif.skills.dao.entity.SkillMountingEntity">
        SELECT <include refid="baseColumn"/>
        FROM `skill_mounting`
        WHERE skill_id = #{skillId} AND is_deleted = 0
        ORDER BY create_time DESC
    </select>

</mapper>
