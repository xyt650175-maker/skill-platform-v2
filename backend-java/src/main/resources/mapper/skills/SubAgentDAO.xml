<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.llmaif.skills.dao.SubAgentDAO">

    <sql id="baseColumn">
        id, agent_id, name, description, system_prompt, model_name,
        is_deleted, create_time, update_time
    </sql>

    <select id="selectByAgentId" resultType="com.company.llmaif.skills.dao.entity.SubAgentEntity">
        SELECT <include refid="baseColumn"/>
        FROM `sub_agent`
        WHERE agent_id = #{agentId} AND is_deleted = 0
        ORDER BY create_time DESC
    </select>

</mapper>
