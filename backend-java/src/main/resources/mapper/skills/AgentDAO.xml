<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.llmaif.skills.dao.AgentDAO">

    <sql id="baseColumn">
        id, name, description, current_version, status, model_name,
        system_prompt, canvas_config, creator_id, is_deleted, create_time, update_time
    </sql>

    <select id="selectByName" resultType="com.company.llmaif.skills.dao.entity.AgentEntity">
        SELECT <include refid="baseColumn"/>
        FROM `agent`
        WHERE name = #{name} AND is_deleted = 0
    </select>

    <select id="selectByCreatorId" resultType="com.company.llmaif.skills.dao.entity.AgentEntity">
        SELECT <include refid="baseColumn"/>
        FROM `agent`
        WHERE creator_id = #{creatorId} AND is_deleted = 0
        ORDER BY create_time DESC
    </select>

    <select id="selectByStatus" resultType="com.company.llmaif.skills.dao.entity.AgentEntity">
        SELECT <include refid="baseColumn"/>
        FROM `agent`
        WHERE status = #{status} AND is_deleted = 0
        ORDER BY update_time DESC
    </select>

</mapper>
