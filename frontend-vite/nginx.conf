server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;
  location /race-api/ { proxy_pass http://llmaif-backend:8080/race-api/; }
  location / { try_files $uri $uri/ /index.html; }
}
